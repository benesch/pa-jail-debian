#!/usr/bin/make -f

DPKG_EXPORT_BUILDFLAGS = 1
include /usr/share/dpkg/default.mk

%:
	dh $@ --sourcedirectory=jail

override_dh_auto_build:
	dh_auto_build -- pa-jail pa-timeout pa-writefifo

override_dh_auto_install:
	dh_auto_install -- PREFIX=/usr
